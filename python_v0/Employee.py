class Employee:
    def __init__(self, name, hours, role):
        self.name = name
        self.hours = hours
        self.role = role


"""
Must take role and assign a tipout percentage based on the variable
if role == barback
    TotalTips.tips * .15 = barback_tips

Must ensure hours is an int

Must ensure the names are not duplicated in shifts


"""
